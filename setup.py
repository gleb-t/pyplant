from setuptools import setup

setup(name='pyplant',
      version='0.3.9',
      description='Python function pipeline',
      packages=['pyplant', 'pyplant.test'],
      zip_safe=False, install_requires=['dateutils'])
